services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - ./fixtures:/app/fixtures:ro
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - FIXTURES_DIR=/app/fixtures
      - DATA_DIR=/app/data
